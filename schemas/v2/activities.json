{
  "title": "activities",
  "description": "Payload of `stride activities` — a list.",
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "avg_hr",
      "date",
      "date_known",
      "distance_m",
      "hard_s",
      "hr_known",
      "id",
      "intensity",
      "intensity_known",
      "load_model",
      "moving_time",
      "name",
      "np_w",
      "power_known",
      "relative_effort",
      "sport",
      "time_known",
      "tss",
      "z1_s",
      "z2_s",
      "z3_s",
      "z4_s",
      "z5_s",
      "zones_known"
    ],
    "additionalKeys": false,
    "properties": {
      "avg_hr": {
        "type": "number"
      },
      "date": {
        "type": "string"
      },
      "date_known": {
        "type": "boolean",
        "description": "Whether `date` could be read. False means the row's stored `start_local` is NULL, empty, malformed, or dated before year 1000 — `date` is then an empty string, which is a HOLE and not a value. Needed because an empty date string already means something else in this contract: SKILL.md documents `last_hard_session_date` as `'' = none on record`, so without this flag a consumer cannot tell 'no such thing exists' from 'it exists and its date is unreadable'. #249 chose to REPORT these rows rather than refuse, because a listing cannot turn a wrong date into a wrong answer, and hoists them to the top so no limit hides them; this is the field that says which ones they are."
      },
      "distance_m": {
        "type": "number"
      },
      "hard_s": {
        "type": "integer"
      },
      "hr_known": {
        "type": "boolean"
      },
      "id": {
        "type": "integer"
      },
      "intensity": {
        "type": "number"
      },
      "intensity_known": {
        "type": "boolean"
      },
      "load_model": {
        "type": "string",
        "enum": [
          "power_stream",
          "weighted_watts",
          "avg_watts",
          "rtss",
          "hr_zones",
          "hr_avg",
          "session_rpe",
          "relative_effort",
          "none",
          ""
        ]
      },
      "moving_time": {
        "type": "integer"
      },
      "name": {
        "type": "string"
      },
      "np_w": {
        "type": "number"
      },
      "power_known": {
        "type": "boolean"
      },
      "relative_effort": {
        "type": "number"
      },
      "sport": {
        "type": "string"
      },
      "time_known": {
        "type": "boolean",
        "description": "Whether the TIME half of the row's stored `start_local` is usable. `date_known AND time_known` is exactly the predicate every ranking site in the engine requires, so this is what tells a consumer that a row with a readable DATE is still refused a ranking position — `2026-08-23T37:00:00Z` is hoisted to the top of `activities` as needing repair and has `date_known: true`. Additive rather than widening `date_known`, which is literally correct about such a row: the date component IS readable, and it is a published boolean SKILL.md tells the coach to trust, so changing its meaning in place would have left the name describing the old one."
      },
      "tss": {
        "type": "number"
      },
      "z1_s": {
        "type": "integer"
      },
      "z2_s": {
        "type": "integer"
      },
      "z3_s": {
        "type": "integer"
      },
      "z4_s": {
        "type": "integer"
      },
      "z5_s": {
        "type": "integer"
      },
      "zones_known": {
        "type": "boolean"
      }
    }
  }
}
