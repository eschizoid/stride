{
  "title": "commands",
  "description": "Answer to a bare `stride --json` (or `--help`/`-h`/`help` under --json): what this binary accepts, and enough about each form to CALL it rather than merely name it (#219). Sourced from the parser's own table, so the answer cannot drift from what actually parses. ADR 0000 §10 declines an MCP server on the grounds that the CLI plus versioned JSON already is the agent interface; this payload is what makes that true. Flags are listed separately — these are subcommands.",
  "type": "object",
  "required": [
    "commands",
    "flags",
    "universal_error_codes"
  ],
  "additionalKeys": false,
  "properties": {
    "commands": {
      "type": "array",
      "description": "One entry per CALLABLE FORM, not per verb. `week` and `week add` are separate entries because they differ in the facts below — one reads and one writes — so a single `week` entry could not answer honestly for either. Subcommands only: `help`, `--help` and `-h` are omitted because they answer WITH this list, and `--version` because it is a flag, listed under `flags`.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "args",
          "mutates",
          "network",
          "interactive",
          "schema",
          "error_codes"
        ],
        "additionalKeys": false,
        "properties": {
          "name": {
            "type": "string",
            "description": "The full callable form, space-separated, exactly as it is typed after `stride` — e.g. \"week add\", \"config set\"."
          },
          "args": {
            "type": "array",
            "description": "This form's own arguments, in the order they are typed, required ones first; an empty array means it takes none. Almost all are positional — `sync`'s `--all` is the one literal flag a command accepts in an argument position, which is why it appears here rather than only in the top-level `flags`. `--json` and `--human` are position-independent and work after any command. `--help` and `--version` are NOT: they parse only as bare invocations, and after a command they are either a usage error or, for a form taking a count, swallowed as that positional — `stride activities --help` answers bad_count.",
            "items": {
              "type": "object",
              "required": [
                "name",
                "required"
              ],
              "additionalKeys": false,
              "properties": {
                "name": {
                  "type": "string",
                  "description": "Placeholder as it appears in usage text, e.g. \"<session_id>\" or \"<asc|desc>\". A literal (no angle brackets) must be passed verbatim."
                },
                "required": {
                  "type": "boolean"
                }
              }
            }
          },
          "mutates": {
            "type": "boolean",
            "description": "Writes to the database. Checked against behaviour rather than declared: the offline e2e suite runs every form with mutates false against a copy of the fixture, at a valid arity taken from this same args list, and fails if the database CONTENTS move. Contents, not file bytes — WAL means a committed write can leave the main file untouched."
          },
          "network": {
            "type": "boolean",
            "description": "Talks to Strava. Since #232 that is exactly `auth` and `sync`. Checked against the source rather than declared, though not by a call-graph analysis: the e2e pins this set as a value, pins `Http.send!` to two functions, and pins those to one module. A command that newly reached them would keep all three green."
          },
          "interactive": {
            "type": "boolean",
            "description": "Blocks waiting on a human. `auth` opens a browser and waits for a paste, so it is the one form an unattended agent must not call. Checked against the source the same way `network` is, with the same limit: the set, the `Stdin` call count and its module are each pinned, but nothing walks the call graph."
          },
          "schema": {
            "type": "string",
            "description": "The file under schemas/v2 this form's success payload validates against, or \"\" when it has no machine payload of its own. Every named file is checked to exist, and every schema in that directory is accounted for — claimed by a form, or pinned by name as one of the three that cannot be (the shared envelope, this payload, and the version payload, which is reached by flag)."
          },
          "error_codes": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Error codes this FORM can return, on top of the top-level `universal_error_codes`. The effective set for a form is the union of the two. NOT claimed to be exhaustive: what is mechanically guaranteed is that every code named here exists in schemas/v2/envelope.json's enum, and that every code in that enum is named by some form or recorded as unattributable — so a new code cannot be added without being attributed, which is the direction such a list normally rots in. Reachability is proven per code by the e2e suite for the codes it can drive."
          }
        }
      }
    },
    "flags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "flags this build accepts. --json, --human, --help and --version are GLOBAL; --all is accepted only by sync, where it forces a full re-list from scratch. The list does not encode that distinction, so a caller should not assume every flag works on every command — a command's own `args` is the authority on its positional arguments and on any flag it accepts in an argument position."
    },
    "universal_error_codes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Error codes ANY form can return, so no form repeats them. The database codes are raised at the process boundary and reachable from every command that opens the database; `usage` is reachable from every form, including argument-less ones, by passing an extra word. `unknown_command` is deliberately absent from this and from every form: it is what you get when there is no form."
    }
  }
}
