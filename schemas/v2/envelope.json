{
  "title": "envelope",
  "type": "object",
  "required": [
    "schema_version"
  ],
  "additionalKeys": false,
  "properties": {
    "schema_version": {
      "type": "integer"
    },
    "data": {},
    "error": {
      "type": "object",
      "required": [
        "code",
        "message"
      ],
      "additionalKeys": false,
      "properties": {
        "code": {
          "type": "string",
          "enum": [
            "activity_already_linked",
            "activity_not_found",
            "activity_required",
            "bad_count",
            "bad_date",
            "bad_id",
            "bad_metric",
            "bad_period",
            "bad_rpe",
            "bad_value",
            "bad_watts",
            "corrupt_database",
            "database_error",
            "derived_key",
            "empty_csv",
            "internal_error",
            "irregular_anchor",
            "missing_client_creds",
            "missing_config",
            "network_unreachable",
            "no_activities",
            "no_activities_csv",
            "no_cp_fit",
            "no_data",
            "no_database",
            "no_detected_intervals",
            "no_intervals_on_date",
            "no_power_data",
            "no_scorable_workouts",
            "no_workout_on_date",
            "not_authenticated",
            "not_set",
            "rate_limited",
            "session_done",
            "session_not_found",
            "stdin_closed",
            "strava_error",
            "unknown_command",
            "unreadable_activity_date",
            "unreadable_config",
            "unreadable_daily_load_day",
            "unreadable_database",
            "unscorable",
            "unzip_failed",
            "usage"
          ],
          "description": "why the command failed. NOTE rate_limited also appears as sync's `stopped` value inside a SUCCESS payload at exit 0, where it means the drain did real work and then hit Strava's cap; as an error code here it means the command made no progress and exits 1. Discriminate on the envelope shape, not on the token (#227)."
        },
        "message": {
          "type": "string"
        }
      }
    }
  }
}
